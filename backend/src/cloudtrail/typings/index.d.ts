import * as Tables from '../../../typings/tables';
import { CloudTrail } from '../../../typings/cloudtrail';

export { Tables, CloudTrail };

export type EVENT_TYPE = Record<string, Tables.EventType>;
